extends layout

block content
  h1= title
  p Welcome to #{title}
  p added a new paragraph
  - var btnType = 'info'
  - var btnSize = 'sm'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) Click here 
